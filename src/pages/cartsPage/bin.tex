{
                  items.map(item => {
                    total += item.productPrice * item.quantity;
                    return (
                      <tr key={item.productID}>
                        <td className=''>
                          <Box p={1} rounded={'md'} className=''>
                            <img src={item.productImage[0]} alt="" className='rounded-md w-[80px] max-h-[80px]'/>
                          </Box>
                          <Box className=''>
                            <Text fontWeight={500} className=''>${item.productName.slice(0, 50)}...</Text>
                          </Box>
                        </td>
                        <td className=' flex gap-2 items-center'>
                          <button type='button' className='rounded-md bg-white w-8 h-8 flex justify-center items-center' onClick={() => decreaseQuantity(item.productID)}><CgMathMinus className='text-sm text-black'/></button>
                          <span className="" >{item.quantity}</span>
                          <button type='button' className='rounded-md bg-white w-8 h-8 flex justify-center items-center' onClick={() => increaseQuantity(item.productID)}><RiAddFill className='text-sm text-black'/></button>
                        </td>
                        <td className=' flex items-center'>
                          <Text fontWeight={500} className='flex items-center'><FaNairaSign/>{(item.productPrice * item.quantity).toLocaleString()}.00</Text>
                        </td>
                        <td className=' flex items-center'>
                          <button className='text-red-500 text-[14px] font-medium' onClick={() => handleRemoveItem(item.productID)}>Remove Item</button>
                        </td>
                        {/* <Box  width={'full'} height={{lx:'90px'}} mb={4} className='rounded-md'>
                          <Box className='flex flex-wrap gap-3 justify-between' >
                          </Box>
                        </Box> */}
                      </tr>
                    );
                  }
                )
                }